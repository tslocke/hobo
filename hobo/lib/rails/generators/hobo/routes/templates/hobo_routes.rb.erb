# This is an auto-generated file: don't edit!
# You can add your own routes in the config/routes.rb file
# which will override the routes in this file.

<%= Rails.application.class.name %>.routes.draw do
<% for subsite in subsites -%>
  <%- if subsite -%>

  <%= "namespace :#{subsite} do" %>
  <%- end -%>
  <%- for controller in controllers_for(subsite) -%>
    <%- router = router_for(subsite, controller) -%>
    <%- for group in %w[ index_action lifecycle resource owner web_method show_action reorder user ] -%>
    <%- gr = "#{group}_routes"
        routes = router.send(gr.to_sym) -%>
    <%- unless routes.empty? -%>

  <%= "  " if subsite %><%= "# #{gr.humanize} for controller :#{controller.controller_name}" %>
    <%- end -%>
      <%- for route in routes -%>
  <%= "  " if subsite %><%= route %>
      <%- end -%>
    <%- end -%>
  <%- end -%>
  <%- if subsite -%>

  end
  <%- end -%>
<%- end -%>

  # TODO point that to the proper location
  match 'dryml/:action', :controller => 'hobo/dryml/dryml_support', :as => 'dryml_support'
  match 'dev/:action', :controller => 'hobo/dev', :as => 'dev_support'

end
